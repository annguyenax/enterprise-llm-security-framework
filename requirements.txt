# Phase 4 — Gateway Skeleton dependencies.
#
# Minimal, lightweight stack only (AGENT_RULES.md rule 11 — no heavy deps
# without approval). Version ranges, not exact pins, to avoid guessing a
# specific patch release that may not exist; narrow the range once the
# team has actually run `pip install` in their own environment.
#
# NOT YET INSTALLED in the agent's working environment as of Phase 4 --
# these packages must be installed by a human running:
#   pip install -r requirements.txt
# before `uvicorn app.main:app --reload` or `pytest` will work.

fastapi>=0.110,<1.0
pydantic>=2.5,<3.0
uvicorn[standard]>=0.27,<1.0
pytest>=7.4,<9.0
httpx>=0.26,<1.0

# Vectorized cosine similarity for workspace hybrid retrieval
# (app/workspace/hybrid_retrieval.py). Added on explicit request; it was
# already present in the local .venv but missing here, so a clean install
# would have failed at import. Wheels are prebuilt for every supported
# platform, so this does not introduce a build toolchain requirement.
numpy>=1.26,<3.0
pypdf>=5.0,<7.0

# python-dotenv intentionally NOT included: config.py reads plain
# environment variables with safe defaults and does not require a .env file
# (see app/core/config.py and README.md "Phase 4 local run").

# Deferred (decision pending, see docs/decisions/ and Phase 5):
# llama-index OR langchain
# chromadb

# Optional / later phase:
# streamlit
