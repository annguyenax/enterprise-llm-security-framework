# Copy this file to .env and fill in real values locally.
# NEVER commit a real .env file. NEVER put real secrets in this .env.example file.
# All values below are placeholders only.

# --- LLM Provider (local by default) ---
# Main evaluation path uses the deterministic Mock Provider (no network call).
LLM_PROVIDER=mock
# Model name the code actually reads is LLM_MODEL_NAME (not LLM_MODEL).
# For local generation via Ollama, set: LLM_PROVIDER=ollama
LLM_MODEL_NAME=qwen3:4b-instruct
LLM_PROVIDER_TIMEOUT_SECONDS=30

# --- Optional Semantic Judge (LLM-as-judge) — default OFF ---
SEMANTIC_GUARD_USE_LLM=0
SEMANTIC_GUARD_MODEL=qwen3:4b

# --- Gateway ---
GATEWAY_HOST=127.0.0.1
GATEWAY_PORT=8000
LOG_LEVEL=INFO

# --- Vector store (decision deferred, see docs/decisions/) ---
VECTOR_STORE_PATH=./data/vector_store
WORKSPACE_EMBEDDING_MODEL=nomic-embed-text
OLLAMA_EMBEDDING_BASE_URL=http://127.0.0.1:11434

# Upload antivirus: off | optional | required. `required` rejects uploads if
# ClamAV is missing or errors. No external scanner is contacted.
UPLOAD_ANTIVIRUS_MODE=optional
CLAMAV_COMMAND=clamscan
CLAMAV_DATABASE_PATH=
CLAMAV_TIMEOUT_SECONDS=20

# --- Logging ---
LOG_FILE_PATH=./logs/gateway.jsonl

# Reminder: any call to a paid API requires explicit user approval
# per AGENT_RULES.md rule 4.
