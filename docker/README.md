# docker/

Docker Compose setup for running the gateway + demo UI + (optional) local services.

**Status: empty — Phase 0 scaffold only.** This is explicitly a **later phase** item per `PROJECT_PLAN.md` — not needed until the application (Phase 3+) exists and is stable enough to containerize.

## Planned Contents (not yet created)

- `docker-compose.yml` — gateway service, demo UI service, optional local vector store service.
- `Dockerfile` — application image.

No containers are defined yet; there is no application to containerize.
